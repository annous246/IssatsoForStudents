<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

</head>
<body><style>
        *{
            padding:0;
            margin:0;
            box-sizing: border-box;
        }
        body{
            text-align: center;
            margin:2em;
        }
        h1{
            border:0.01em solid black ;
            padding:1em;
            transition: 0.5s;;
        }
        h1:hover{
            border:0.01em solid ;
            padding:1em;
            color:white;
            background-color: black;
            border:0.01em solid white;
            transition: 0.5s;;
        }
        form{
            display: flex;
            flex-direction:column;
            margin:2em;

            padding:1em;
        }
        input{
            padding:1em;
            margin:2em;

        }
        button{
            padding: 1em;
            background-color: rgb(83, 125, 189,1);
            border:none;
            border-radius: 0.2em;
            transition: 0.5s;;
        }
        button:hover{
            padding: 1em;
            background-color: rgb(83, 125, 189,0.8);
            border:none;
            border-radius: 0.2em;
            transition: 0.5s;;
        }
        a{
            margin-top:2em;
            text-decoration: none;
            color:black;
        }
        .sign{
            padding: 1em;
            background-color: rgb(83, 125, 189,1);
            border:none;
            border-radius: 0.2em;
            width: 50%;
            transition: 0.5s;;


        }
        .sign:hover{
            padding: 1em;
            background-color: rgb(83, 125, 189,0.8);
            border:none;
            border-radius: 0.2em;
            transition: 0.5s;;

        }
        #filler{
            padding: 1em;
            font-size: 0.7em;
            margin-top: 1em;
            border: solid black 0.08em;
            transition: 0.2s all;
        }
        #filler:hover{
            border: solid white 0.08em;
            color: white;
             background-color: black;
            transition:0.2s all;

        }
        #msg{
            padding: 1em;
            margin: 1em;
        }
        .linker{
            position: relative;
            display: flex;
            flex-direction: column;
            flex-wrap: nowrap;
            justify-content: center;
            align-items: center;
            text-align: center;
            transition: 0.02s all;

        }
    </style>
    
    <a href="home"><h1>Login page</h1></a>
    <form action="/login" method="post" id="lform">
        <label for="email">Email</label>
        <input name ="email" type="email" placeholder="Email" required>
        <label for="password">Password</label>
        <input name ="password"  type="password" placeholder="*********" required>
        <button id="log" type="submit">Log In</button>
    </form>
    <p id="msg"><%=message.msg%></p>
    <div class="linker">
    <a href="register" class="sign">Sign Up</a>
    <a id="filler" href="forgot">Forgot Password</a>
    </div>

    </div><script src="/javascript/LogDOM.js" type="text/javascript"></script>
</body>
</html>